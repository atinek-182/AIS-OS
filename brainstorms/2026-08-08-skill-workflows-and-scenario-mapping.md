# Skill Workflows & Scenario Mapping: Brainstorm / Discovery Notes
Date: 2026-08-08 · Goal: Discover and map multi-skill workflows, skill chains, and optimal skill selection for different engineering, design, client automation, and research scenarios.

## Summary / key decisions
- **Focus Area**: Comprehensive Multi-Scenario Skill Mapping across all 65+ workspace skills.
- **Active Workflow Pipeline**: Autonomous Session Post-Mortem & Self-Healing Engine (`/session-postmortem-audit` -> `/global-rules-engine` -> `sync_global_rules.py` -> `/verify-design`).

## Q&A Log
### Q1 — Web Design & Visual Experience System Pipeline
- Asked: Which primary scenario workflow would you like to detail and map first?
- Captured: Web Design & Visual Experience System (Component catalog -> Hallmark anti-slop -> GSAP/WebGL -> Playwright QA).
- Flags: Resolved -> Atinek Maurya selected.

### Q2 — Component Sourcing & Brand Adaptation
- Asked: How should the Web Design Workflow source and adapt UI components?
- Captured: Query-First Synthesis. Always query `design_synthesis_engine.py` first to locate and adapt existing tested components into ZORIXEL brand standards before writing code.
- Flags: Resolved -> Atinek Maurya selected.

### Q3 — Specialized Workflow Selection
- Asked: Which of the 4 specialized scenario workflows would you like to run or test next?
- Captured: Autonomous Session Post-Mortem & Self-Healing (`/session-postmortem-audit` -> `/global-rules-engine` -> `/verify-design`).
- Flags: Resolved -> Atinek Maurya selected.

### Q4 — Advanced Specialized Chains (MCP, Composio SaaS, Remotion Video, Autoresearch, Excalidraw Handoff)
- Asked: Deep exploration of 5 new high-leverage advanced scenario chains.
- Captured: Defined 5 specialized advanced workflow matrices below.

---

## 5 Advanced Specialized Scenario Chains

### 1. MCP Server & Application Engineering Chain
```
Target Tool / Protocol Brief
  └── /build-mcp-server (Designs MCP tools & schema definitions)
  └── /build-mcpb (Packages server into standalone binary/bundle)
  └── /build-mcp-app (Adds interactive UI widgets & DOM interfaces)
  └── /mcp-integration (Integrates into workspace mcp_config.json)
```

### 2. Multi-Platform SaaS Automation Chain
```
Client Stack Brief (HubSpot, Stripe, Linear, Shopify, GAS, Notion)
  └── /composio (Executes OAuth & SaaS actions across 1000+ apps)
  └── /gws-automation-engine (Provisions Google Sheets, Drive, & GAS scripts)
  └── /notion-sync + /notion-formula-builder (Builds Notion master databases & dynamic formulas)
  └── /zero-paywall-client-os (Assembles 100% zero-subscription client hub)
```

### 3. React Video & Motion Graphics Engine Chain
```
Video Brief / Motion Graphics Concept
  └── /canvas-design (Generates base visual assets & graphics)
  └── /gsap-animation (Defines physics keyframes & scroll triggers)
  └── /remotion (Renders programmatic React videos)
  └── /carousel-render (Outputs HD PNG/MP4 media assets)
```

### 4. Autoresearch & System Observability Chain
```
Codebase Performance / Optimization Target
  └── /storm-research-project (7-agent multi-perspective analysis)
  └── /smart-explore (Tree-sitter AST structural search)
  └── /autoresearch-manage (Runs autonomous optimization loop)
  └── /graphify (Updates tree-sitter AST knowledge graph)
```

### 5. Visual Architecture & Executive Handoff Chain
```
Technical Architecture / Pitch Brief
  └── /excalidraw-diagram (Generates visual system topology diagram)
  └── /frontend-slides (Builds 16:9 interactive HTML presentation deck)
  └── /interactive-operator-guide-generator (Builds copyable SOP dashboard)
  └── /wowerpoint (Outputs executive PDF slide deck)
```

## Open Flags (Pending Input)
- None. All 10 scenario workflow chains fully mapped and documented.
