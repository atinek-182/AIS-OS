---
title: F4 Step Sequence
domain: skill
summary: Numbered stages (`1.0 → 2.0 → 3.0`) flow vertically. Each stage has a heading, a paragraph, sometimes a small visual.
  *Use when:* the product is a workflow, not a single moment.
critical_directives:
- Follow document guidance without bypassing established safeguards.
section_outline:
- F4 · Step sequence
read_triggers:
- When working on skill in .agents/skills/hallmark/references/components/f4-step-sequence.md
- When reading context for F4 Step Sequence
tags:
- skill
- f4-step-sequence
updated: '2026-08-08'
---

### F4 · Step sequence
Numbered stages (`1.0 → 2.0 → 3.0`) flow vertically. Each stage has a heading, a paragraph, sometimes a small visual.
*Use when:* the product is a workflow, not a single moment.
*Don't confuse with:* F2 Sticky-scroll (which doesn't number stages).

```html
<ol class="steps">
  <li><span class="stage">1.0</span><h3>Intake.</h3><p>…</p></li>
  <li><span class="stage">2.0</span><h3>Plan.</h3><p>…</p></li>
</ol>
```
